<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SuGarToolkit.Sample.Dialogs.Views.MessageBoxSamplesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SuGarToolkit.Sample.Dialogs.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SuGarToolkit.Sample.Dialogs.ViewModels"
    mc:Ignorable="d">

    <NavigationView
        x:Name="RootNavigation"
        x:FieldModifier="private"
        IsBackButtonVisible="Collapsed"
        IsBackEnabled="False"
        IsPaneToggleButtonVisible="False"
        IsSettingsVisible="False"
        MenuItemsSource="{x:Bind viewModel.SampleItems}"
        PaneDisplayMode="Top"
        SelectedItem="{x:Bind viewModel.SampleItems[0]}"
        SelectionChanged="NavigationView_SelectionChanged">

        <NavigationView.MenuItemTemplate>
            <DataTemplate x:DataType="vm:NavigationItem">
                <NavigationViewItem Content="{x:Bind Name}" />
            </DataTemplate>
        </NavigationView.MenuItemTemplate>

        <Frame
            x:Name="ContentFrame"
            x:FieldModifier="private"
            IsNavigationStackEnabled="False" />

    </NavigationView>

</Page>
