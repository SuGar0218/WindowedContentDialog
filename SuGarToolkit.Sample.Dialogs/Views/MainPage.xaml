<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SuGarToolkit.Sample.Dialogs.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SuGarToolkit.Sample.Dialogs.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SuGarToolkit.Sample.Dialogs.ViewModels"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="TitleBarArea"
            Grid.Row="0"
            x:FieldModifier="private">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <StackPanel
                Grid.Column="0"
                HorizontalAlignment="Left"
                Orientation="Horizontal">
                <Button
                    Height="36"
                    Click="{x:Bind ToggleNavigationPane}"
                    Style="{ThemeResource TitleBarPaneToggleButtonStyle}" />
                <TextBlock
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="SuGarToolkit.Controls.Dialogs" />
            </StackPanel>
            <Button
                Grid.Column="0"
                Width="48"
                Height="30"
                Padding="0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="Transparent"
                BorderThickness="0"
                Click="ThemeToggleButton_Click"
                CornerRadius="0">
                <FontIcon FontSize="16" Glyph="&#xE793;" />
            </Button>
            <Rectangle
                x:Name="TitleBarRightInset"
                Grid.Column="1"
                x:FieldModifier="private" />
        </Grid>

        <NavigationView
            x:Name="RootNavigationView"
            Grid.Row="1"
            x:FieldModifier="private"
            IsBackButtonVisible="Collapsed"
            IsBackEnabled="False"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            MenuItemsSource="{x:Bind viewModel.SampleList}"
            SelectionChanged="RootNavigationView_SelectionChanged">
            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="vm:NavigationItem">
                    <NavigationViewItem Content="{x:Bind Name}" Icon="Placeholder" />
                </DataTemplate>
            </NavigationView.MenuItemTemplate>
            <Frame
                x:Name="ContentFrame"
                x:FieldModifier="private"
                IsNavigationStackEnabled="False" />
        </NavigationView>
    </Grid>
</Page>
